name: CI/CD Pipeline

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  lint-and-unit:
    name: Lint & Unit Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'resume-site/package-lock.json'
          
      - name: Install Dependencies
        working-directory: ./resume-site
        run: npm ci
        
      - name: Run Linter
        working-directory: ./resume-site
        run: npm run lint
        
      - name: Run Unit Tests & Coverage
        working-directory: ./resume-site
        run: npm run test:coverage

  e2e-and-a11y:
    name: Playwright E2E & A11y
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'resume-site/package-lock.json'
          
      - name: Install Dependencies
        working-directory: ./resume-site
        run: npm ci
        
      - name: Install Playwright Browsers
        working-directory: ./resume-site
        run: npx playwright install --with-deps

      - name: Build Next.js (required for `next start` webServer)
        working-directory: ./resume-site
        run: npm run build

      - name: Run Playwright Tests
        working-directory: ./resume-site
        run: npx playwright test
        
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: resume-site/playwright-report/
          retention-days: 14

  visual-regression:
    name: Lost-Pixel Visual Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'resume-site/package-lock.json'
          
      - name: Install Dependencies
        working-directory: ./resume-site
        run: npm ci

      # lost-pixel bundles its own playwright-core (v1.47.2 → chromium build 1134)
      # which is a different version from @playwright/test installed at the top level.
      # Install via lost-pixel's bundled binary so the right Chromium revision is fetched.
      - name: Install Playwright Chromium for lost-pixel
        working-directory: ./resume-site
        run: node ./node_modules/lost-pixel/node_modules/playwright-core/cli.js install --with-deps chromium

      - name: Build Next.js
        working-directory: ./resume-site
        run: npm run build

      - name: Start Next.js server & Run Lost-Pixel
        working-directory: ./resume-site
        run: |
          npm run start &
          npx wait-on http://localhost:3000
          npx lost-pixel
